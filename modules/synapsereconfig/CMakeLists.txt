IF (NOT ENABLE_SYNAPSERECONFIG)

        SET(ENABLE_SYNAPSERECONFIG 0 CACHE BOOL "Enable the synapse reconfiguration module")

ENDIF()

IF (ENABLE_SYNAPSERECONFIG)

        SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_SYNAPSERECONFIG=1 PARENT_SCOPE)

        SET(CAER_HW_FILES modules/synapsereconfig/synapsereconfig.c)

        SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_HW_FILES} PARENT_SCOPE)

ENDIF()
