IF (NOT ENABLE_FPGASPIKEGEN)
    SET(ENABLE_FPGASPIKEGEN 0 CACHE BOOL "Enable the FPGA spike generator module")
ENDIF()

IF (ENABLE_FPGASPIKEGEN)
	IF(DYNAPSEFX2)
        SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_FPGASPIKEGEN=1 PARENT_SCOPE)

		SET(CAER_HW_FILES modules/fpgaspikegen/fpgaspikegen.c)

		SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_HW_FILES} PARENT_SCOPE)
	ELSE()
		MESSAGE("NOT COMPILING HELLOWORLD AS IT DEPENDS ON THE DYNAPSEFX2 MODULE")
	ENDIF()
ENDIF()
