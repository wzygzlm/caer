IF (NOT ENABLE_POISSONSPIKEGEN)
	SET(ENABLE_POISSONSPIKEGEN 0 CACHE BOOL "Enable the poisson spike generator module")
ENDIF()

IF (ENABLE_POISSONSPIKEGEN)
	IF(DYNAPSEFX2)
		SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_POISSONSPIKEGEN=1 PARENT_SCOPE)

		SET(CAER_HW_FILES modules/poissonspikegen/poissonspikegen.c)

		SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_HW_FILES} PARENT_SCOPE)
	ELSE()
		MESSAGE("NOT COMPILING POISSONSPIKEGEN AS IT DEPENDS ON THE DYNAPSEFX2 MODULE")
	ENDIF()
ENDIF()
