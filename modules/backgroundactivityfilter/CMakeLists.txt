ADD_LIBRARY(bafilter SHARED backgroundactivityfilter.c)

SET_TARGET_PROPERTIES(bafilter
	PROPERTIES
	PREFIX "caer_"
)

TARGET_LINK_LIBRARIES(bafilter ${CAER_LIBS})

INSTALL(TARGETS bafilter DESTINATION ${CAER_MODULES_DIR})
