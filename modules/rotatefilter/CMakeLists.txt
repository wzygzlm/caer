IF (NOT ROTATE)
	SET(ROTATE 0 CACHE BOOL "Enable the rotation module")
ENDIF()

IF (ROTATE)
	ADD_LIBRARY(rotate SHARED rotatefilter.c)

	SET_TARGET_PROPERTIES(rotate
		PROPERTIES
		PREFIX "caer_"
	)

	TARGET_LINK_LIBRARIES(rotate ${CAER_C_LIBS})
ENDIF()
