IF (NOT ENABLE_SPIKEFEATURES)
	SET(ENABLE_SPIKEFEATURES 0 CACHE BOOL "Enable the spike features filter")
ENDIF()

IF (ENABLE_SPIKEFEATURES)
	SET(CAER_COMPILE_DEFINITIONS ${CAER_COMPILE_DEFINITIONS} -DENABLE_SPIKEFEATURES=1 PARENT_SCOPE)

	SET(CAER_SF_FILES modules/spikefeatures/spikefeatures.c)

	SET(CAER_C_SRC_FILES ${CAER_C_SRC_FILES} ${CAER_SF_FILES} PARENT_SCOPE)
ENDIF()
